import React from 'react';
import NavigationItems from '../navigation/navigationItems';


const NavBar = ( props ) => (
    <header>
        <nav>
            <NavigationItems />
        </nav>
    </header>
);

export default NavBar;